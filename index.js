
/**
 * Module dependencies.
 */

// var fs = require('fs');

/**
 * Expose `entityRecognition`.
 */

module.exports = entityRecognition;

/**
 * A one-liner.
 *
 * @param {String} name
 * @return {Function}
 * @api public
 */

function entityRecognition(name) {

}
